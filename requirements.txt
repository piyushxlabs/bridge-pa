# Bridge-PA — Python Backend Dependencies
# Install: pip install -r requirements.txt
# Python 3.12 required.
# NOTE: All version pins removed — pip auto-resolves the full dependency tree.

langchain-google-genai
langchain-core
langgraph
langgraph-checkpoint-postgres
asyncpg
psycopg-binary
fastapi
uvicorn[standard]
python-dotenv
httpx
pydantic
structlog
sse-starlette
pytest
pytest-asyncio
google-api-core